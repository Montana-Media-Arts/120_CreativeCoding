function setup() {
	createCanvas(800,400);
	background(230);

	// CONSTANT VARS are defined before the sketch runs
	// These include, variabls such as...
	text(PI, width * 0.5, height * 0.5);

	// p5, sets "environment variables"
	// In this case, width=>"equals the canvas width"
	// height=>"The canvas height"
	translate( width * 0.25, height * 0.25  );
	ellipse(0,0,60,60);

}

function draw() {

}
