function setup() {
    // NUMBER 1 thing to do
    createCanvas(500,500);
    background(0,253,242);

    // This is a "Line Comment"
    // Line comments are preceeded by two forward slashes.
    fill(0, 0, 198); // Line comments can also come at the end of a line of code.
    stroke(0); // Sometimes these can be used to describe the line
    strokeWeight(16);
    // Sometimes you would place the comment above the line
    ellipse(250,250,300,120);


    /*
    This is a Block Comment. Block comments are written with the forward-slash star combo. 

     */


    // Ellipse 1
    // This is a purple ellipse in the middle of the screen-ish
    fill(240, 0, 198);
    stroke(255);
    strokeWeight(16);
    ellipse(150,200,300,120);


    fill(0, 100, 0);
    stroke(150);
    strokeWeight(16);
    ellipse(350,350,300,120);


}
